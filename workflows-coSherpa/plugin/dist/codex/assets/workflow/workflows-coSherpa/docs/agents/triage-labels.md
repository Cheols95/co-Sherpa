# Triage labels (for engineering skills)

> **Single source of truth: `AGENTS.md` → "Agent-skills configuration" → "Triage roles"**
> (auto-loaded each session).
>
> This file is an intentionally-thin **path-slot** for the deferred `triage` skill. Do NOT restate
> the role/label table here — a second copy only drifts (doc-hygiene rule, `AGENTS.md` §"문서 위생").
> Read AGENTS.md.
>
> (Removed 2026-06-15: the old "Category roles: bug, enhancement" line — a phantom convention that
> nothing in the pipeline emitted or consumed.)
