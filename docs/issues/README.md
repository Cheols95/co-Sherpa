# docs/issues/

`/to-issues`(issue-tracker=local) 산출 **수직 슬라이스 목록**을 둔다. 파일명 `NNN-<slug>.md` (예: `001-auth-bootstrap.md`).

주의: 여기의 슬라이스는 **목록**이지 FCG goal이 아니다. `/fcg-goal`이 이를 읽어 `goals/<n>-*.{md,gates.sh,next-task.sh}` 3파일 실행계약으로 변환한다.
