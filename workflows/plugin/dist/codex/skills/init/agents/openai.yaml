interface:
  display_name: "ironman init"
  short_description: "Install or sync the ironman workflow harness."
  default_prompt: "Use ironman init to install the workflow here."

policy:
  allow_implicit_invocation: false
