interface:
  display_name: "co-Sherpa init"
  short_description: "Install or sync the co-Sherpa workflow harness."
  default_prompt: "Use cosherpa init to install the workflow here."

policy:
  allow_implicit_invocation: true
