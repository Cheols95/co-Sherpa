interface:
  display_name: "co-Sherpa migration"
  short_description: "Onboard an existing codebase into the co-Sherpa harness."
  default_prompt: "Use cosherpa migration to fill project context from this codebase."

policy:
  allow_implicit_invocation: true
