# 세션 보고서 — 2026-06-14~15 (워크플로우 응집도·로드구조 정비)

> 내일 이어서 작업하기 위한 핸드오프. **이 파일은 이 템플릿 레포 *개발용* 핸드오프**이지 프로젝트
> 자산이 아니다 — 새 프로젝트로 복사할 땐 삭제해도 된다. 전모는 메모리
> `template-cohesion-session-2026-06.md`.

## 세션 종료 (2026-06-15) — "다음 세션 할 일" 정산

- **task#1 @import 검증 → 완료.** Claude Code 하위폴더 `@import` 자동로드 작동 확인
  (메모리 `template-subdir-import-loadcheck`).
- **task#2 OPEN finding → 완료.** `script-cohesion-libs`(B, `68468d2`·`1de8f46`),
  `doc-prose-dedup`(A, `b1c4585`·`9026875`) 종료. `minor-cohesion-cleanup`(b)은
  **WONTFIX**(중복 경미 — 사용자 결정).
- **task#3(선택) → 폐기.** 이슈 `README`→`AGENTS` frontmatter 이전·가이드 동기화는
  진행 안 함(사용자 결정 2026-06-15).
- **전역 스킬 sync**: `install-skills --force`로 repo `skills/`(A 편집분 포함)를 전역 반영.

→ 이 핸드오프의 "다음 세션 할 일"은 전부 정산(완료/폐기). 아래는 이력으로 보존.

## 오늘 한 일 (커밋 순)

1. **3시스템 비교**(BMAD/dryforge/mpfcg) — 결론: *검증형 게이트*(mpfcg/dryforge)가 모델 발전과 함께
   스케일 + 회귀안전 → 승철 최우선 기준 부합. **mpfcg 백본 유지** 권고. BMAD는 완주친화·프런트엔드
   (PRFAQ) 강점이나 검증=LLM판정·인지부하 큼.
2. **첫 build META_CHECKS 자동화** (`c3db9d3`) — build 모드B 절차 + diagnose 백스톱(real goal 있는데
   `_meta` 비면 경고).
3. **응집도 감사 + finding 6건** (`c3db9d3`) — 배포 드리프트 6스킬 해소(전역이 구버전이라 `/grill`이
   영어 원본으로 돌던 버그), prototype·improve-arch 한국어화, docs/agents 포인터 축소, grilling 분류
   모순 해소, 그래프린트→graph-lint.
4. **finding ①⑤⑥ 처리** (`e73f700`) — engine-glyph ASCII(P1 **CLOSED**, cp949 면역), tracker 노트
   (**CLOSED**), minor(partial: kind·DEEPENING 닫음, cycle통합 보류).
5. **tdd 스킬 → guidelines/tdd 이전** (`f0e7576`) — 스킬 간 자동 호출이 없음을 확인 → build가 닿도록
   guidelines로 옮기고 goal-iteration이 §참조. skills/tdd 제거. finding **CLOSED**.
6. **AGENTS.md 로드구조 정비** (이번 커밋) — 아래.

## 이번 커밋: AGENTS 축약 + @import 로드구조

**확인된 로드 규칙(중요)**: 자동 로드되는 건 **루트뿐**. Claude Code는 `AGENTS.md`를 안 읽고
`CLAUDE.md`만 본다(+하위 CLAUDE.md는 그 폴더 파일 Read 시 downward 로드, `@import`도 expand). Codex는
`AGENTS.md`를 upward(root→cwd)로만. 스킬이 참조하는 일반 .md는 자동 로드 아님 — "필독" 지시 시 능동 Read.

- 루트 `AGENTS.md` **170→89줄**: Phase1 elicitation → `docs/grill/AGENTS.md`(신설), Gate validity +
  risk heuristic → `goals/AGENTS.md`로 이전. **doc-hygiene·spec authority·경로·FCG 불변식·triage는 유지**
  (철학 뼈대 = 매 세션 각인).
- 루트 `CLAUDE.md`: 자연어 "Read AGENTS.md" → **`@AGENTS.md` import**(Claude도 자동 로드 보장).
- AGENTS.md 있는 4폴더(goals·cycles·docs/findings·docs/grill)에 `CLAUDE.md`(`@AGENTS.md` 한 줄) 생성 —
  Claude Code downward 로드용.
- 스킬 §참조 새 위치로 갱신 + **grill/freeze에 "작업 전 필독" 명시**(능동 로드 보장). build는 risk·gate를
  goals/AGENTS.md로.
- manifest에 신규 파일(docs/grill/AGENTS.md, 4× CLAUDE.md) 등재.

## 다음 세션 할 일

### 1. @import 실작동 검증 (우선 — 이번 변경의 핵심 가정)
- 다음 세션에서 `goals/` 또는 `docs/grill/` 파일을 Read할 때 그 폴더 `CLAUDE.md`(`@AGENTS.md`)가 실제로
  로드되는지 확인(Claude Code downward import). 안 되면 구조 재고.
- `/grill`·`/freeze` 호출 시 `docs/grill/AGENTS.md`를 실제로 읽는지(자연어 "작업 전 필독"을 따르는지).

### 2. 남은 OPEN finding (`docs/findings/2026-06-14T1600-*`)
- **`script-cohesion-libs`** (P2) — `_goals-lib.sh` 추출(goal 열거/frontmatter/issue-id 중복 통합). 가장
  손이 많이 가는 작업 → `/build cycles/...`로 묶어 처리 권장.
- **`doc-prose-dedup`** (P2) — **재점검 필요**: AGENTS 축약으로 정본 위치는 이동했으나, A(의도감사)는
  `freeze:28-37`·`to-spec`(동결 전 게이트)에, B(risk close-out)는 `build:44-47`에 **여전히 전문 복제**.
  각각 1줄 gist + `§`인용으로 축소. (C 사다리는 손대지 말 것 — 이미 인용형.)
- **`minor-cohesion-cleanup`** (b만 남음) — `prompts/cycle-generate.md` ↔ `skills/cycle/SKILL.md` 절차
  통합(경미).

### 3. 선택 (낮은 우선)
- `docs/issues/README.md`의 frontmatter 계약을 `docs/issues/AGENTS.md`로 옮길지(현재 risk heuristic만
  goals로 갔고, frontmatter 계약은 README + 루트 AGENTS에 분산).
- `Workflow_Guideline_v1.html`에 이번 AGENTS 구조 변경 미반영(스킬 13→12종은 갱신함). 가이드 동기화.

## 상태
- 커밋: `c3db9d3` · `e73f700` · `f0e7576` · (이번 v1.6) — 모두 main, push 완료.
- 메모리: `template-cohesion-session-2026-06.md` (이번 세션 전모 + 환경 정정 + 3시스템 결론).
